# 可选：HTTP 代理（在公司网络 / 沙箱环境访问 PubMed 时填写，形如 http://127.0.0.1:7890）
PUBMED_PROXY=

# 每次检索返回条数（默认 20）
RETMAX=20

# 可选：接入 OpenAI 兼容 LLM 后，回答升级为生成式（不填则走离线抽取式，无需任何密钥）
LLM_API_KEY=
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4o-mini
